{
	'description': '${perspectiveName} perspective',
#if($includeLaunchpad)
	'extensionPoint': 'launchpad-${projectName}-perspective',
#else
	'extensionPoint': 'launchpad-${launchpadName}-perspective',
#end
	'module': '${projectName}/ui/${perspectiveName}/extensions/perspective/perspective.js'
}